import { createTheme } from '@material-ui/core/styles';
const theme = createTheme({
  palette: {
    green: {
      light: '#9fe9d8',
      main: '#4AC0A4'
    },
    gold: {
      main: '#BC2D5F'
    },
    grey: {
      main: '#333333'
    }
  },
});

export default theme;
